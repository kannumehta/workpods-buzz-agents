# Hermes' standard OpenAI Responses transport uses this value only as the
# bearer token for the internal WorkPods LLM gateway.
OPENAI_API_KEY=REPLACE_WITH_HERMES_GATEWAY_TOKEN
OPENAI_BASE_URL=http://workpods-llm-gateway:8080/v1
